# Example environment file for the frontend (Vite).
# Copy this to `.env` and update as needed for local development.

VITE_BACKEND_URL=http://localhost:3001

# Notes:
# - Keep real runtime secrets out of the repo. Use .env (ignored) for local overrides.